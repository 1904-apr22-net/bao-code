git init
git clone
git log
git push

git add <path>
    this will stage all local changes
    to my files/folders at/under that path
    usually: "git add ." from the top-level repo folders
    (because "." in bash means, the current directory)

//